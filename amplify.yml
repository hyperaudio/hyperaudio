version: 1
applications:
  - appRoot: website
    backend:
      phases:
        preBuild:
          commands:
            - pip3.8 install --user pipenv
            - pip3.8 install --user virtualenv
        build:
          commands:
            - amplifyPush --simple
    frontend:
      phases:
        preBuild:
          commands:
            - yarn
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*

