version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - yarn
            - pip3 install --user pipenv
            - pip3 install --user venv
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
    appRoot: website
